# Data

This folder stores source policy inputs used by the policy builder.

Each category subfolder can contain plain text, CSV, or JSON files with country values and CIDR values.

Example file names:

1. `countries.txt`
2. `cidrs.txt`
3. `source.csv`

Lines that start with `#` are ignored.